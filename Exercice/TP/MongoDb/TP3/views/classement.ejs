<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Classement du jeu</title>
  <style>
    table { border-collapse: collapse; width: 60%; margin: 20px auto; }
    th, td { border: 1px solid #aaa; padding: 8px; text-align: center; }
    th { background-color: #ddd; }
    h1 { text-align: center; }
  </style>
</head>
<body>
  <h1>Classement des joueurs</h1>
  <table>
    <thead>
      <tr>
        <th>Joueur</th>
        <th>Tentatives</th>
        <th>Temps (secondes)</th>
        <th>Nombre à trouver</th>
      </tr>
    </thead>
    <tbody>
      <% if (sessions.length === 0) { %>
        <tr><td colspan="4">Aucune partie terminée pour le moment</td></tr>
      <% } else { %>
        <% sessions.forEach(session => { %>
          <tr>
            <td><%= session.username %></td>
            <td><%= session.attempts %></td>
            <td><%= session.timeSpent %></td>
            <td><%= session.numberToFind %></td>
          </tr>
        <% }) %>
      <% } %>
    </tbody>
  </table>
</body>
</html>
